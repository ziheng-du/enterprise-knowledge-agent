# 项目背景
这是一个"企业新员工知识助手"项目,融合 RAG 检索与 Agent 自主决策能力。项目定位是求职作品集,代码质量、架构清晰度、可扩展性与功能本身同等重要,请始终以"可维护、可讲解"为标准写代码,而不是"能跑就行"。

# 技术栈(严格遵守,不要擅自替换或引入未提及的库)
- Python 3.10+
- Agent编排:LangGraph(状态图模式,禁止用简单 if-else 链替代状态图逻辑)
- RAG:LangChain
- 向量数据库:Chroma(本地持久化模式)
- Web框架:FastAPI(路由用 APIRouter,请求/响应体必须用 Pydantic BaseModel 定义)
- 服务启动:uvicorn
- 日志:标准库 logging,禁止用 print 做调试输出
- 测试:pytest

# 架构硬性约束(这是本项目的核心设计意图,任何代码生成都不能违反)

1. **工具层必须通过抽象基类统一接入**
   所有工具必须继承 `tools/base.py` 中定义的工具基类,包含 name、description、input_schema(Pydantic模型)、run() 方法。
   Agent 决策逻辑(agent/graph.py)只能通过 `tools/registry.py` 动态获取工具列表,禁止在 Agent 逻辑里直接 import 具体工具函数。
   原因:未来某个工具要改造成 MCP Server 时,只需要在外层包一层协议适配,不能因为耦合太深导致大改。写工具相关代码时,注释里要体现这个设计意图。

2. **检索模块必须与 Agent 逻辑解耦**
   `rag/retriever.py` 必须能被单独调用测试,不能依赖 LangGraph 的上下文或状态对象。
   RAG 的检索(retriever)和生成回答的逻辑要分开,不要写成一个大函数糅在一起。

3. **Agent 状态用 LangGraph 的 StateGraph 显式建模**
   状态字段在 `agent/state.py` 中用 TypedDict 清晰定义(用户输入、检索结果、工具调用历史、当前轮次、最终回答等),不要用普通字典隐式传递状态。

4. **必须有工具调用轮次上限和兜底逻辑**
   - 设置最大工具调用轮次(默认3轮),防止死循环
   - 工具调用参数解析失败时要有重试或降级逻辑,不能让异常直接冒泡导致服务崩溃
   - 检索不到相关文档时,必须在 Prompt 中明确约束模型如实告知"未找到相关信息",禁止编造答案

# 代码规范

- 所有函数写 docstring,说明输入、输出、用途,尤其是 Agent 节点函数和工具的 run() 方法
- 边界情况(空输入、API超时、工具返回异常格式)要显式处理并写注释说明处理逻辑,禁止裸的 except: pass
- 敏感信息(API_KEY、BASE_URL)一律从环境变量读取(通过 config.py 统一管理),禁止硬编码在代码里,新增配置项要同步更新 .env.example
- Prompt 模板集中放在 agent/prompts.py,不要在业务逻辑代码里散落硬编码的提示词字符串
- 新增依赖必须同步添加到 requirements.txt,并说明这个依赖是做什么用的

# 工作方式约束

- 每次只实现我明确要求的那个阶段/模块,不要自动"顺手"帮我实现后续阶段的内容,即使你觉得逻辑上应该一起做
- 生成代码后,如果涉及架构性决策(比如某个模块该怎么拆分、要不要新增中间层),先用一两句话说明理由,不要直接下结论式地改代码然后不解释
- 如果发现 PROJECT_SPEC.md 里的要求和当前代码实现有冲突或不清楚的地方,先提出问题,不要自行假设并直接生成代码
- 涉及 LangGraph 的图结构设计、状态定义这类关键架构点,优先说明设计思路,再给代码,不要一次性甩一大段代码不加解释

# 明确禁止

- 禁止引入 Streamlit、Gradio 等一体化框架替代 FastAPI + 前端分离的架构
- 禁止跳过工具抽象层直接写"能跑的" hack 代码
- 禁止生成假的/占位的 README 内容,README 必须真实反映当前已实现的功能
- 禁止在没有征得同意的情况下修改 requirements.txt 里已锁定的框架选型(LangGraph/LangChain/FastAPI/Chroma)
